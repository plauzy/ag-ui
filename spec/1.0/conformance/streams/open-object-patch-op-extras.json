{
  "name": "open-object-patch-op-extras",
  "area": "processing",
  "description": "where the schema deliberately leaves an object open, an undescribed member is not unrecognised material: a JSON Patch remove carrying a leftover value arrives whole, applies, and draws no warning",
  "specPage": "/spec/1.0/basic/processing",
  "kill": "delete the specOpen branch in the strip stage \u2014 the state still reduces the same way, so eventPaths on the delivered STATE_DELTA is what proves the leftover member arrived whole rather than being quietly removed",
  "stream": [
    {
      "type": "RUN_STARTED",
      "threadId": "t-patch-extras",
      "runId": "r-patch-extras"
    },
    {
      "type": "STATE_SNAPSHOT",
      "snapshot": {
        "items": [
          "keep-a",
          "drop-me",
          "keep-b"
        ]
      }
    },
    {
      "type": "STATE_DELTA",
      "delta": [
        {
          "op": "remove",
          "path": "/items/1",
          "value": "drop-me"
        }
      ]
    },
    {
      "type": "RUN_FINISHED",
      "threadId": "t-patch-extras",
      "runId": "r-patch-extras"
    }
  ],
  "expect": {
    "outcome": "completed",
    "eventTypes": [
      "RUN_STARTED",
      "STATE_SNAPSHOT",
      "STATE_DELTA",
      "RUN_FINISHED"
    ],
    "noWarnings": true,
    "state": {
      "items": [
        "keep-a",
        "keep-b"
      ]
    },
    "eventPaths": {
      "2.delta.0.value": "drop-me",
      "2.delta.0.op": "remove"
    }
  }
}
