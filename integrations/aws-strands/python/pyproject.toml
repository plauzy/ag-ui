[project]
name = "ag_ui_strands"
description = "Implementation of the AG-UI protocol for AWS Strands Agents."
readme = "README.md"
license = "MIT"
version = "0.4.0"
license-files = ["LICENSE"]
authors = [
    { name = "AG-UI Contributors" }
]
requires-python = ">=3.10, <3.15"
dependencies = [
    "ag-ui-a2ui-toolkit>=0.0.4",
    "ag-ui-protocol>=0.1.22",
    "fastapi>=0.115.12",
    "pydantic>=2.11.2",
    "strands-agents>=1.15.0",
]

[project.urls]
Changelog = "https://github.com/ag-ui-protocol/ag-ui/releases"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ag_ui_strands"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=1.3.0",
    # The provider-binding tests run the native history through the real
    # formatters the Strands SDK ships. bedrock and llamacpp need nothing extra
    # (boto3 is a strands dependency, llamacpp speaks HTTP), and those two cover
    # one formatter from each family on their own. openai is installed as well
    # because it is the formatter whose 400 the adjacency rule is about, so the
    # suite asserts against the provider that actually rejects the bad shape.
    # The remaining six are exercised when their SDK happens to be present.
    # Pinned to the range strands-agents itself supports for this provider, so
    # the suite exercises a formatter/SDK pairing the SDK actually claims.
    "openai>=1.68.0,<3.0.0",
    # The HTTP-layer tests drive the endpoint over real requests: httpx backs
    # fastapi's TestClient, and uvicorn serves the disconnect tests, which need
    # a real socket to close.
    "httpx>=0.28.1",
    "uvicorn>=0.38.0",
]
