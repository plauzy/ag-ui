[tool.poetry]
name = "aws-strands-server"
version = "0.1.0"
description = "Strands integration server for AG-UI with multi-provider support"
authors = ["AG-UI Contributors"]
readme = "README.md"
packages = [{ include = "server" }]

[tool.poetry.dependencies]
python = "<3.15,>=3.10"
ag-ui-a2ui-toolkit = ">=0.0.4"
fastapi = "^0.115.12"
pydantic = ">=2.11.2"
python-dotenv = "^1.0.0"
uvicorn = ">=0.34.3"
strands-agents = {extras = ["openai", "anthropic", "gemini"], version = "^1.35.0"}
ag_ui_strands = {path = "..", develop = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
dev = "server:main"
