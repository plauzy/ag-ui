// <auto-generated />
// @generated by spec/generator — DO NOT EDIT.
// Source: https://ag-ui.com/spec/1.0/schema.json
// Regenerate: pnpm --filter @ag-ui/spec generate
#nullable enable

using System;
using System.IO;

namespace AGUI.Protobuf;

// The envelope pre-scan, generated from the same wire model as the
// TypeScript scan: different known event kinds in one envelope are rejected.
// Repeated occurrences of one kind use protobuf's normal merge behavior.
// Unknown field numbers are protobuf's to ignore,
// repeated or not, so forward compatibility is untouched. Field zero is not a
// legal tag. Legacy group fields nest and are skipped wholesale.
internal static class WireGuards
{
    private static readonly bool[] KnownEnvelopeTags = BuildKnownTags();

    private static bool[] BuildKnownTags()
    {
        var known = new bool[32];
        known[1] = true;
        known[2] = true;
        known[3] = true;
        known[4] = true;
        known[5] = true;
        known[6] = true;
        known[7] = true;
        known[8] = true;
        known[9] = true;
        known[10] = true;
        known[11] = true;
        known[12] = true;
        known[13] = true;
        known[14] = true;
        known[15] = true;
        known[16] = true;
        known[17] = true;
        known[18] = true;
        known[19] = true;
        known[20] = true;
        known[21] = true;
        known[22] = true;
        known[23] = true;
        known[24] = true;
        known[25] = true;
        known[26] = true;
        known[27] = true;
        known[28] = true;
        known[29] = true;
        known[30] = true;
        known[31] = true;
        return known;
    }

    /// <summary>
    /// Rejects an envelope whose malformed shape would decode differently across runtimes,
    /// and reports whether what it carries could be an event newer than this build.
    /// </summary>
    /// <returns>
    /// <see langword="true"/> only when the envelope names no event this build knows and
    /// carries at least one unknown length-delimited field — the shape every event arm of
    /// the envelope has, and so the only shape a newer runtime's event can arrive in.
    /// Anything else that leaves the decoder without an event is broken bytes rather than
    /// a variant from the future, and the two are answered differently.
    /// </returns>
    public static bool AssertWellFormedEnvelope(ReadOnlySpan<byte> data)
    {
        int knownTags = 0;
        uint knownEventField = 0;
        int unknownEventArms = 0;
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                // A group carrying a known event's field number still names that
                // event, and counting it keeps a decoder that finds no readable
                // event from mistaking these bytes for an event from the future.
                if (groupDepth == 0 && field < (uint)KnownEnvelopeTags.Length && KnownEnvelopeTags[(int)field])
                {
                    knownTags += 1;
                }

                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            if (groupDepth == 0 && field < (uint)KnownEnvelopeTags.Length && KnownEnvelopeTags[(int)field])
            {
                // Repeated occurrences of the same event merge. A different
                // known event kind still violates the envelope's contract.
                if (knownEventField != 0 && knownEventField != field)
                {
                    throw new InvalidDataException(
                        "Invalid event: the envelope carries more than one event.");
                }

                knownTags += 1;
                knownEventField = field;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    // Two statements on purpose: adding the call inline would
                    // capture offset before the varint advances it.
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0
                        && (field >= (uint)KnownEnvelopeTags.Length || !KnownEnvelopeTags[(int)field]))
                    {
                        // A length-delimited field this build does not know is
                        // the shape a newer runtime's event arrives in.
                        unknownEventArms += 1;
                    }

                    if (groupDepth == 0)
                    {
                        // Events that can carry a nested oneof get their
                        // payload scanned for arm exclusivity too.
                        switch (field)
                        {
                        case 1:
                            ScanTextMessageStartEvent(data.Slice(offset, (int)length));
                            break;
                        case 2:
                            ScanTextMessageContentEvent(data.Slice(offset, (int)length));
                            break;
                        case 3:
                            ScanTextMessageEndEvent(data.Slice(offset, (int)length));
                            break;
                        case 4:
                            ScanToolCallStartEvent(data.Slice(offset, (int)length));
                            break;
                        case 5:
                            ScanToolCallArgsEvent(data.Slice(offset, (int)length));
                            break;
                        case 6:
                            ScanToolCallEndEvent(data.Slice(offset, (int)length));
                            break;
                        case 7:
                            ScanStateSnapshotEvent(data.Slice(offset, (int)length));
                            break;
                        case 8:
                            ScanStateDeltaEvent(data.Slice(offset, (int)length));
                            break;
                        case 9:
                            ScanMessagesSnapshotEvent(data.Slice(offset, (int)length));
                            break;
                        case 10:
                            ScanRawEvent(data.Slice(offset, (int)length));
                            break;
                        case 11:
                            ScanCustomEvent(data.Slice(offset, (int)length));
                            break;
                        case 12:
                            ScanRunStartedEvent(data.Slice(offset, (int)length));
                            break;
                        case 13:
                            ScanRunFinishedEvent(data.Slice(offset, (int)length));
                            break;
                        case 14:
                            ScanRunErrorEvent(data.Slice(offset, (int)length));
                            break;
                        case 15:
                            ScanStepStartedEvent(data.Slice(offset, (int)length));
                            break;
                        case 16:
                            ScanStepFinishedEvent(data.Slice(offset, (int)length));
                            break;
                        case 17:
                            ScanTextMessageChunkEvent(data.Slice(offset, (int)length));
                            break;
                        case 18:
                            ScanToolCallChunkEvent(data.Slice(offset, (int)length));
                            break;
                        case 19:
                            ScanSubagentStartedEvent(data.Slice(offset, (int)length));
                            break;
                        case 20:
                            ScanSubagentFinishedEvent(data.Slice(offset, (int)length));
                            break;
                        case 21:
                            ScanSubagentErrorEvent(data.Slice(offset, (int)length));
                            break;
                        case 22:
                            ScanToolCallResultEvent(data.Slice(offset, (int)length));
                            break;
                        case 23:
                            ScanActivitySnapshotEvent(data.Slice(offset, (int)length));
                            break;
                        case 24:
                            ScanActivityDeltaEvent(data.Slice(offset, (int)length));
                            break;
                        case 25:
                            ScanReasoningStartEvent(data.Slice(offset, (int)length));
                            break;
                        case 26:
                            ScanReasoningMessageStartEvent(data.Slice(offset, (int)length));
                            break;
                        case 27:
                            ScanReasoningMessageContentEvent(data.Slice(offset, (int)length));
                            break;
                        case 28:
                            ScanReasoningMessageEndEvent(data.Slice(offset, (int)length));
                            break;
                        case 29:
                            ScanReasoningMessageChunkEvent(data.Slice(offset, (int)length));
                            break;
                        case 30:
                            ScanReasoningEndEvent(data.Slice(offset, (int)length));
                            break;
                        case 31:
                            ScanReasoningEncryptedValueEvent(data.Slice(offset, (int)length));
                            break;
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }

        return knownTags == 0 && unknownEventArms > 0;
    }

    // The nested scans, one message level each, generated from the shared
    // scan graph. A oneof carrying more than one distinct arm in an occurrence
    // rejects. Repeated message fields merge through the standard parser.
    // google.protobuf.* payloads remain outside the guard's traversal.
    private static void ScanTextMessageStartEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanBaseEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanTextMessageContentEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanTextMessageEndEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCallStartEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCallArgsEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCallEndEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanStateSnapshotEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanStateDeltaEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 2:
                                ScanJsonPatchOperation(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanJsonPatchOperation(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanMessagesSnapshotEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 2:
                                ScanMessage(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanMessage(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 5:
                                ScanToolCall(data.Slice(offset, (int)length));
                                break;
                            case 8:
                                ScanInputContent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCall(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanInputContent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;
        uint seenArm = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {

                        if (field == 1 || field == 2 || field == 3 || field == 4 || field == 5)
                        {
                            if (seenArm != 0 && seenArm != field)
                            {
                                throw new InvalidDataException(
                                    "Invalid event: part carries more than one arm.");
                            }

                            seenArm = field;
                        }
                        switch (field)
                        {
                            case 1:
                                ScanTextInputPart(data.Slice(offset, (int)length));
                                break;
                            case 2:
                                ScanImageInputPart(data.Slice(offset, (int)length));
                                break;
                            case 3:
                                ScanAudioInputPart(data.Slice(offset, (int)length));
                                break;
                            case 4:
                                ScanVideoInputPart(data.Slice(offset, (int)length));
                                break;
                            case 5:
                                ScanDocumentInputPart(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanTextInputPart(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanImageInputPart(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanInputContentSource(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanInputContentSource(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;
        uint seenArm = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {

                        if (field == 1 || field == 2 || field == 3)
                        {
                            if (seenArm != 0 && seenArm != field)
                            {
                                throw new InvalidDataException(
                                    "Invalid event: source carries more than one arm.");
                            }

                            seenArm = field;
                        }
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanAudioInputPart(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanInputContentSource(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanVideoInputPart(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanInputContentSource(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanDocumentInputPart(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanInputContentSource(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanRawEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanCustomEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanRunStartedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 5:
                                ScanRunAgentInput(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanRunAgentInput(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 5:
                                ScanMessage(data.Slice(offset, (int)length));
                                break;
                            case 6:
                                ScanTool(data.Slice(offset, (int)length));
                                break;
                            case 9:
                                ScanResumeEntry(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanTool(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanResumeEntry(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanRunFinishedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 6:
                                ScanInterrupt(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanInterrupt(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            default:
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanRunErrorEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanStepStartedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanStepFinishedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanTextMessageChunkEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCallChunkEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanSubagentStartedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanSubagentFinishedEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanSubagentErrorEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanToolCallResultEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 7:
                                ScanInputContent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanActivitySnapshotEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanActivityDeltaEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                            case 5:
                                ScanJsonPatchOperation(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningStartEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningMessageStartEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningMessageContentEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningMessageEndEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningMessageChunkEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningEndEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    private static void ScanReasoningEncryptedValueEvent(ReadOnlySpan<byte> data)
    {
        int offset = 0;
        int groupDepth = 0;

        while (offset < data.Length)
        {
            uint tag = ReadVarint(data, ref offset);
            uint field = tag / 8;
            uint wireType = tag % 8;

            if (field == 0)
            {
                throw new InvalidDataException("Invalid event: field zero is not a legal tag.");
            }

            if (wireType == 3)
            {
                groupDepth += 1;
                continue;
            }

            if (wireType == 4)
            {
                groupDepth -= 1;
                if (groupDepth < 0)
                {
                    throw new InvalidDataException("Invalid event: unbalanced group.");
                }

                continue;
            }

            switch (wireType)
            {
                case 0:
                    ReadVarint(data, ref offset);
                    break;
                case 1:
                    offset += 8;
                    break;
                case 2:
                {
                    uint length = ReadVarint(data, ref offset);
                    if (offset + (long)length > data.Length)
                    {
                        throw new InvalidDataException("Invalid event: truncated payload.");
                    }

                    if (groupDepth == 0)
                    {
                        switch (field)
                        {
                            case 1:
                                ScanBaseEvent(data.Slice(offset, (int)length));
                                break;
                        }
                    }

                    offset += (int)length;
                    break;
                }
                case 5:
                    offset += 4;
                    break;
                default:
                    throw new InvalidDataException("Invalid event: unknown wire type.");
            }

            if (offset > data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated payload.");
            }
        }

        if (groupDepth != 0)
        {
            throw new InvalidDataException("Invalid event: unbalanced group.");
        }
    }

    // Mirrors the uint32 reader truncation exactly: the fifth byte contributes
    // only its low four bits, later bytes none — otherwise an overlong tag
    // encoding would dodge duplicate detection while the real decoder still
    // reads the canonical field number.
    private static uint ReadVarint(ReadOnlySpan<byte> data, ref int offset)
    {
        uint result = 0;
        int shift = 0;
        while (true)
        {
            if (offset >= data.Length)
            {
                throw new InvalidDataException("Invalid event: truncated varint.");
            }

            byte value = data[offset++];
            if (shift < 28)
            {
                result += (uint)(value & 0x7f) << shift;
            }
            else if (shift == 28)
            {
                result += (uint)(value & 0x0f) << 28;
            }

            shift += 7;
            if ((value & 0x80) == 0)
            {
                return result;
            }
        }
    }
}
