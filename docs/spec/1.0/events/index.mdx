---
title: "Event Streams"
description: "The eight event families, and who reacts to each — 1.0"
---

Everything a producer has to say arrives as events, in eight families. Each
family binds one of the [event patterns](/spec/1.0/basic/patterns) and adds
its own rules; each is addressed to a different part of the consumer:

| Family | Events | Pattern | Consumed by |
| --- | --- | --- | --- |
| [Runs and steps](/spec/1.0/events/lifecycle) | `RUN_STARTED` · `RUN_FINISHED` · `RUN_ERROR` · `STEP_STARTED` · `STEP_FINISHED` | lifecycle | the client itself |
| [Text messages](/spec/1.0/events/text-messages) | `TEXT_MESSAGE_*` | streaming | the UI |
| [Tool calls](/spec/1.0/events/tool-calls) | `TOOL_CALL_*` | streaming | the application |
| [Reasoning](/spec/1.0/events/reasoning) | `REASONING_*` | streaming | the UI |
| [State](/spec/1.0/events/state) | `STATE_SNAPSHOT` · `STATE_DELTA` · `MESSAGES_SNAPSHOT` | snapshot–delta | the application store |
| [Activity](/spec/1.0/events/activity) | `ACTIVITY_SNAPSHOT` · `ACTIVITY_DELTA` | snapshot–delta | the UI |
| [Subagents](/spec/1.0/events/subagents) | `SUBAGENT_STARTED` · `SUBAGENT_FINISHED` · `SUBAGENT_ERROR` | lifecycle | the client itself |
| [Passthrough](/spec/1.0/events/passthrough) | `RAW` · `CUSTOM` | standalone | the application, opt-in |

The thirty-one event types these families comprise are enumerated on
[The Event Model](/spec/1.0/basic), and their shapes are the
[schema](/spec/1.0/schema)'s. Only the run lifecycle is mandatory for a
producer — every other family is a feature it emits when it has something to
say with it. A consumer MUST accept all of them: an event from a family a
consumer has no use for is still a well-formed event, not
[unrecognised material](/spec/1.0/basic/processing).

<CardGroup cols={2}>
  <Card title="Runs and steps" href="/spec/1.0/events/lifecycle">
    When a run begins and ends, and how steps mark its phases.
  </Card>
  <Card title="Text messages" href="/spec/1.0/events/text-messages">
    The conversation itself, streamed as it is generated.
  </Card>
  <Card title="Tool calls" href="/spec/1.0/events/tool-calls">
    The agent proposes, the application disposes.
  </Card>
  <Card title="Reasoning" href="/spec/1.0/events/reasoning">
    The model's thinking, streamed, and the encrypted artefacts that
    round-trip it.
  </Card>
  <Card title="State" href="/spec/1.0/events/state">
    Shared state and the conversation, replaced and amended.
  </Card>
  <Card title="Activity" href="/spec/1.0/events/activity">
    Structured progress a UI renders outside the transcript.
  </Card>
  <Card title="Subagents" href="/spec/1.0/events/subagents">
    Attribution, ownership, nesting, and how delegated work terminates.
  </Card>
  <Card title="Raw and custom events" href="/spec/1.0/events/passthrough">
    The escape hatches, and their limits.
  </Card>
</CardGroup>
